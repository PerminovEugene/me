import React from "react";

const CVSubHeader = () => {
  return (
    <React.Fragment>
      <span></span>
      <button
        // onClick={() => downloadPDF}
        className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
      >
        Download PDF
      </button>
    </React.Fragment>
  );
};
export default CVSubHeader;
